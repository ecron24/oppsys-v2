---
description: Generate service in front for api
---

# Task: Generate service in front for api

Based on @apps/client/src/components/modules/service/modules-service.ts apps/client/src/components/modules/service/modules-types.ts , create service for $ARGUMENTS and put in #file:$ARGUMENTS-service.ts #file:$ARGUMENTS-types.ts.

Api for $ARGUMENTS is #file:$ARGUMENTS-router.ts.

- Check if #file:$ARGUMENTS-service.ts already exist, if so complete that, if not create.
